{# purpose of this template: services of location view json view #}
[
{% for serviceOfLocation in items %}
    {% if not loop.first %},{% endif %}
    {{ serviceOfLocation.toArray()|json_encode() }}
{% endfor %}
]
