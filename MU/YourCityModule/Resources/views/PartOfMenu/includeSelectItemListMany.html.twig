{# purpose of this template: inclusion template for display of related parts of menu #}
<ul id="{{ idPrefix }}ReferenceList">
{% if items is defined and items is iterable %}
{% for item in items %}
{% set idPrefixItem = idPrefix ~ 'Reference_' ~ item.getKey() %}
<li id="{{ idPrefixItem }}">
    {{ item|muyourcitymodule_formattedTitle }}
     <a id="{{ idPrefixItem }}Remove" href="javascript:mUYourCityRemoveRelatedItem('{{ idPrefix }}', '{{ item.getKey() }}');"><span class="fa fa-trash-o"></span></a>
</li>
{% endfor %}
{% endif %}
</ul>
