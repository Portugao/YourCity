{# purpose of this template: menus of location view csv view #}
{% spaceless %}"{{ __('Name') }}";"{{ __('Description') }}";"{{ __('Image of menu') }}";"{{ __('Position of menu') }}";"{{ __('Effectiv from') }}";"{{ __('Effectiv until') }}";"{{ __('Kind of menu') }}";"{{ __('Additional remarks') }}";"{{ __('Workflow state') }}"
;"{{ __('Location') }}"
;"{{ __('Dishes') }}";"{{ __('Parts of menu') }}"
{% endspaceless %}
{% for menuOfLocation in items %}
{% spaceless %}
    "{{ menuOfLocation.name }}";"{{ menuOfLocation.description|safeHtml }}";"{{ menuOfLocation.imageOfMenu }}";"{{ menuOfLocation.positionOfMenu }}";"{{ menuOfLocation.effectivFrom|localizeddate('medium', 'short') }}";"{{ menuOfLocation.effectivUntil|localizeddate('medium', 'short') }}";"{{ menuOfLocation.kindOfMenu|muyourcitymodule_listEntry('menuOfLocation', 'kindOfMenu') }}";"{{ menuOfLocation.additionalRemarks|safeHtml }}";"{{ menuOfLocation.workflowState|muyourcitymodule_objectState(false)|lower }}"
    ;"{% if menuOfLocation.location|default %}{{ menuOfLocation.location|muyourcitymodule_formattedTitle }}{% endif %}"
    ;"
    {% if menuOfLocation.dishes|default %}
        {% for relatedItem in menuOfLocation.dishes %}
        {{ relatedItem|muyourcitymodule_formattedTitle }}{% if not loop.last %}, {% endif %}
        {% endfor %}
    {% endif %}
    ";"
    {% if menuOfLocation.partsOfMenu|default %}
        {% for relatedItem in menuOfLocation.partsOfMenu %}
        {{ relatedItem|muyourcitymodule_formattedTitle }}{% if not loop.last %}, {% endif %}
        {% endfor %}
    {% endif %}
    "
{% endspaceless %}
{% endfor %}
